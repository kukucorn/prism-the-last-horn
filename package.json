{
  "name": "prism-the-last-horn",
  "version": "0.1.0",
  "description": "The last unicorn gathers 7 elemental colors. A js13kGames precision platformer.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "pack": "node scripts/roadroll.js",
    "zip": "npm run build && npm run pack && cd dist && zip -9 -X -q game.zip index.html && node ../scripts/size.js"
  },
  "devDependencies": {
    "roadroller": "^2.1.0",
    "terser": "^5.37.0",
    "vite": "^6.0.7",
    "vite-plugin-singlefile": "^2.1.0"
  }
}
